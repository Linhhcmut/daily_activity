import 'package:flutter/material.dart';

class FotgotPasswordPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Fotgot page"),
      ),
    );
  }
}
