import 'package:flutter/material.dart';

class Body1Profile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget> [
          SizedBox(height: 100),
          Text(
            "1",
            style: TextStyle(color: Colors.black),
          ),
        ],
      ),
    );
  }
}
